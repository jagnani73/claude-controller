# Deploy / Caddy config (read by scripts/start.ts + Caddyfile).
# Copy to .env and fill in. `!` = required. See README "Run / setup" for details.

CONTROLLER_HOST!
TAILSCALE_IP!

# Optional — the Caddyfile has defaults for these.
# BACKEND_ADDR
# FRONTEND_DIST
